spring:
  application:
    name: backend
  datasource:
    url: jdbc:h2:mem:adventurebook;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

